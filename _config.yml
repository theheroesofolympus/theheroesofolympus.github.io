title: "Anubhav"
author: "KMIT Students"
description: "Classroom blogs where students can share their ideas."

theme: "minima"
github_username: "KMIT-Students"

website: https://kmit-students.github.io/base-demo
# basewebsite: /blog-demo

kramdown:
  syntax_highlighter_opts:
    disable: true
    
plugins:
  - jekyll-sitemap

titles_from_headings:
  strip_title: true
  collections: true

defaults:
  - scope:
      path: ""
      type: post
    values:
      tags: All
